#include <stdio.h>
#include "main.h"

/**
 * main - print the name of program 
 * @argc:count argument 
 * @argv:arguments
 *
 * Return:Always 0 (success)
 */

int main(int argc, _attribute_((unused)) char *argv[])
{
	int i
	for (i = 0; i < argc; i++)
	{
	}
	printf("%d\n", i - 1);
	return (0);
}
